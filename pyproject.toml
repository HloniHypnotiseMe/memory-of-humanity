[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "memory-of-humanity"
version = "0.1.0"
description = "A protocol for humanity to remember itself."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
memory-of-humanity = "node.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
