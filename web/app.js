const $=id=>document.getElementById(id); const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function get(path){const r=await fetch(path);const j=await r.json();if(!r.ok)throw Error(j.error||'Request failed');return j} async function post(path,body){const r=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const j=await r.json();if(!r.ok)throw Error(j.error||'Request failed');return j}
async function search(){const q=$('search').value.trim();if(!q){$('results').innerHTML='<div class="muted">Search for a memory, person, place, story or phrase.</div>';return}const j=await get('/api/search?q='+encodeURIComponent(q));$('results').innerHTML=j.results.map(x=>'<article class="result"><div><span class="pill">'+esc(x.record_type)+'</span><span class="pill">'+esc(x.epistemic_status)+'</span></div><p>'+esc(x.record.content.text)+'</p><small>'+esc(x.id)+' · contributor '+esc(x.record.provenance.contributor_id)+'</small></article>').join('')||'<div class="muted">No matching memories.</div>'}
async function albums(){const j=await get('/api/albums');$('albums').innerHTML=j.albums.map(a=>'<article class="result"><h3>'+esc(a.title)+'</h3><p>'+esc(a.description||'No description yet.')+'</p><small>'+esc(a.id)+' · '+a.items.length+' item(s)</small></article>').join('')||'<div class="muted">No albums yet.</div>'}
$('save').onclick=async()=>{const text=$('memory').value.trim();if(!text)return;$('saveState').textContent=' Preserving…';try{const j=await post('/api/memories',{contributor_id:$('contributor').value.trim(),record_type:$('recordType').value,epistemic_status:$('status').value,text});$('memory').value='';$('saveState').textContent=' Preserved '+j.id;await search()}catch(e){$('saveState').textContent=' '+e.message}};
$('search').oninput=()=>{clearTimeout(window._t);window._t=setTimeout(search,220)};$('albumsRefresh').onclick=albums;$('refresh').onclick=()=>location.reload();$('discover').onclick=async()=>{$('federation').textContent=JSON.stringify(await get('/api/discovery'),null,2)};albums();